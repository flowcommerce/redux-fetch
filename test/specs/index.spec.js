import * as ReduxFetch from '../../src/index';

// This test also serves as an entry point to import
// the entire library for coverage reporting.

describe('ReduxFetch', () => {
  it('should have exports', () => {
    expect(ReduxFetch).to.exist;
  });
});
